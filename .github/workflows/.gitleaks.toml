[allowlist]
  description = "允许某些安全的字符串"
  regexes = [
    'example.com',
    'test-token'
  ]

[[rules]]
description = "Generic Password"
regex = '''(?i)(password|pwd|pass)[:=]\s*['"]?[A-Za-z0-9!@#$%^&*()_+=-]{6,}['"]?'''
tags = ["password"]
